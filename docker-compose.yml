version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/workspaces/GusKitchenAI
    command: python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload